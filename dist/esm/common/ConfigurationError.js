export class ConfigurationError {
    constructor(reason) {
        this.event = -1;
        this.reason = "unknown reason";
        this.reason = reason;
    }
}
//# sourceMappingURL=ConfigurationError.js.map